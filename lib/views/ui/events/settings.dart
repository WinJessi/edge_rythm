import 'package:flutter/material.dart';

class EventsSettings extends StatelessWidget {
  const EventsSettings({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
